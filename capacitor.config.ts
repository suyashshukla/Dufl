import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kavyastrot.dufl',
  appName: 'dufl',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
